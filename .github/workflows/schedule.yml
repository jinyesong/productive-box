name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.32cd341e6113e4cddb205ab41c48316f }}
          TIMEZONE: Asia/Seoul
